

export default function renderRows(rowItem, pokemon) {
    const tableRow = document.createElement('tr');

    const nameData = document.createElement('td');
    nameData.classList.add = 'align-left';
    
}